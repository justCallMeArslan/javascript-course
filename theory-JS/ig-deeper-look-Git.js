// part of lesson is in ./git-pg-curriculum

// git commit --amend = retrieves last commit to replace it with new one (only unpushed files)

// git rebase (-i e.g) allows us to choose multiple commits and stage them for 
// correction/ removal and other updates